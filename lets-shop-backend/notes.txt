type : module //allow to use import statements rather than ""const app = require("app")""
Dependencies - npm i dotenv : used to load environment variables.
 "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.19",
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }